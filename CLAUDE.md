# Guitar Lessons (Woodshed Lab)

Personal project. Solo developer, no review gate.

## Git workflow

- **Commit directly to `main`.** Do not create branches unless I explicitly ask for one. This overrides the branch-per-change / branch-naming convention in the global CLAUDE.md for this repo.
- Same as global: never `git commit` or `git push` unless I ask — show the diff first.
