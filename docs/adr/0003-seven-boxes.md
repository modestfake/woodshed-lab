# Seven boxes per scale, not nine

Three-notes-per-string yields exactly seven distinct shapes — one starting on each
scale degree — which then repeat at the octave. The original brief described "9
boxes"; those were octave repeats of boxes 1 and 2 drawn to fill the neck, not
distinct shapes. `BOX_COUNT` is 7 on purpose — don't "restore" the missing two.
